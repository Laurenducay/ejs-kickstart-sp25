<%- include('../partials/header') %>
    <h1><%=title%></h1>
    <p>This page is a demo built using Express.Js and EJS templates. The following page demonstrates what we learned in class, which is a list/view design with several users. The data aquired is pulled from a JSON file with random user info. The pictures were generated by third party APIss, and does not include actual information pertaining to the identites.</p>   
<div class="container">
    <table class="table table-hover table-striped">
        <thead>
            <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Email</th>
            </tr>
        </thead>
        <tbody>
            <% users.forEach(function(user){ %>
            <tr>
                <td><a href="users/view/<%= user.id %>"><%= user.first_name %></a></td>
                <td><%= user.last_name %></td>
                <td><%= user.email %></td>
            </tr>
            <% }); %>
        </tbody>
    </table>
</div>
<%- include('../partials/footer') %>